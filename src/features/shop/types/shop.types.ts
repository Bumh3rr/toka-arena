export type ShopTab = 'store' | 'wallet'

/** Disponibilidad de compra derivada del catálogo. */
export type ItemAvailability = 'buyable' | 'soon'

/** Filtro de categoría dentro de la Tienda. */
export type StoreFilter = 'all' | 'acc' | 'eggs' | 'specials'
